import React from "react";
import Layout from "./Components/Layout";

const Index = ({ auth, contactdetails }) => {
    return <Layout auth={auth} contactdetails={contactdetails} />;
};

export default Index;
